import 'package:flutter/material.dart';

class AttendanceCheckPage extends StatelessWidget {
  const AttendanceCheckPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trang điểm danh (dành cho Giảng viên)'),
    );
  }
}
